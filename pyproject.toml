[project]
name = "hexagonal"
version = "0.1.0"
description = "hexagonal agrège l'information électorale et administrative française"
authors = [
    { name = "Salomé Cheysson", email = "salome@cheysson.fr" }
]
readme = "README.md"
requires-python = "==3.14.*"
dependencies = [
    "dvc>=3.55.2",
    "dvc-s3>=3.2.0",
    "glom>=24.11.0",
    "pandas>=2.2.2",
    "geopandas>=1.0.1",
    "click>=8.1.7",
    "pyarrow>=20.0.0",
    "lxml>=6.0.0",
    "pyzstd>=0.16.2",
    "pydantic>=2.11.7",
    "polars>=1.38.3",
    "pyroaring>=1.0.4",
    "jinja2>=3.1.4",
    "tomli-w>=1.1.0",
    "python-slugify[unidecode]>=8.0.4",
    "py7zr>=0.22.0",
    "json-stream>=2.3.2",
    "openpyxl>=3.1.5",
    "boto3>=1.42.19",
    "cvxpy>=1.8.2",
    "fastexcel>=0.19.0",
]

[dependency-groups]
dev = [
    "ipython>=8.26.0",
    "pytest>=8.3.4",
    "ruff>=0.8.4",
    "pyrefly>=0.56.0",
    "jupyterlab>=4.5.7",
    "altair[all]>=6.1.0",
    "great-tables>=0.21.0",
]

[tool.hexagonal]
s3_profile = "hexagonal"
s3_root = "s3://hexagonal-106475418133-eu-west-3-an/"
http_root = "https://hexagonal-106475418133-eu-west-3-an.s3.eu-west-3.amazonaws.com"

[tool.pytest.ini_options]
addopts = [
    "--import-mode=importlib",
]

[tool.ruff]
line-length = 88

[tool.ruff.lint]
select = ["E", "W", "F", "I", "N", "B", "A", "PD"]

[build-system]
requires = ["uv_build>=0.11.18,<0.12"]
build-backend = "uv_build"

[tool.uv.build-backend]
source-exclude = ["src/hexagonal/data"]
source-include = ["src/scripts/**"]


[tool.pyrefly]
project-includes = [
    "src/**/*.py*",
]
